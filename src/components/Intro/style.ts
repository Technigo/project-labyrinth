import styled from "styled-components";

const IntroContainer = styled.div`
  padding: 1rem;
  display: grid;
  justify-content: center;
`;

const Text = styled.p`
  color: #045687;
  font-size: 1.5em;
`;

const ButtonContainer = styled.div`
  text-align: center;
`;

export { ButtonContainer, IntroContainer, Text };
