/*

export const selectAction = (type, direction) => {
  return (dispatch, getState) => {
    fetch('https://labyrinth.technigo.io/action')
      .then((response) => response.json())
      .then((json) => {
        dispatch(game.actions.game(json));

        dispatch(game.actions.loading(false));
      });
  };
};

*/
