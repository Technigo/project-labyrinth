/*

export const quotes = (props) => {
 const quote = useSelector((store) => store.quotes.quote)
 const quote = props.quote
 const dispatch = useDispatch(tag)
 const onTagButtonClick = () => {
    dispatch()

 }
 return 
 <div>
 <h1> quote: </h1>
 <p> {quote.content} </p>
 {quote.tags.map((singletag) => <button key={singleTag}> type="button"> {singleTag}</button>
}

export default quotes


const url = `länk${getState().quotes.author}${tag ? `länk${tag}}` : "")
fetch(url)
.then.response => response.json ());
.then((quote) => dispatch(quotes.actions.setquote(quote)));

*/

