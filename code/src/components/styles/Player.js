// when using styled-components/macro the classnames show
// so it's not random names as styled-components generate automatically
import styled from 'styled-components/macro';

export const Player = styled.img`
  border-radius: 50%;
  overflow: hidden;
  background-color: hotpink;
  width: 5rem;
`;