import React from 'react';

export const Description = () => {
  /*  const description = useSelector((state) => state.XXXXXX) */
  return (
    <div>
      <h1>Description</h1>
      {/*  {description.map((d) => (
         <>
          <p>{d.description}</p>
          <p>{d.coordinates}</p>
        </>
      ))} */}
    </div>
  );
};
